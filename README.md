# Sticky-TabSZ

Make sure all URLs matching a certain pattern open in one tab (either an already existing tab matching the tab pattern, or a new one if one does not exists)

Supports matching within the same Multi-Account Containers or ignoring difference of containers and reloading the existing tab (within it's container).

